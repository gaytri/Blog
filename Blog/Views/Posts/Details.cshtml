@model Blog.Models.Post

@{
    ViewBag.Title = "Details";
    bool isAdmin = ViewBag.IsAdmin ?? false;
}

<!-- Use the post helper to render the details of this post -->
@PostHelper.Render(Model, Html, isAdmin, true)
